apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geoss-els.fullname" . }}-test-connection"
  labels:
    {{- include "geoss-els.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "geoss-els.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
