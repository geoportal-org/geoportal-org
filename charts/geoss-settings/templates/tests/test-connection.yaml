apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geoss-settings.fullname" . }}-test-connection"
  labels:
    {{- include "geoss-settings.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "geoss-settings.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
