import { Flex } from "@chakra-ui/react";
import { TextContent } from "../TextContent/TextContent";

export const LoadingErrorInfo = () => (
    <Flex fontSize="s">
        <TextContent id="information.error.loading" />
    </Flex>
);
