export enum ButtonType {
    BUTTON = "button",
    SUBMIT = "submit",
    RESET = "reset",
}

export enum ButtonVariant {
    OUTLINE = "geossOutline",
    GHOST = "geossGhost",
    SOLID = "geossSolid",
}
